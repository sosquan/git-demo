hello git!```git config --global user.name 用户名```
hello git!```git config --global user.name 用户名```
hello git!
hello git!```git config --global user.name 用户名```
hello git!```git config --global user.name 用户名```
hello git!```git config --global user.name 用户名```
hello git!```git config --global user.name 用户名```
hello git!```git config --global user.name 用户名```
hello git!```git config --global user.name 用户名```
